#! /usr/bin/env node

(async function () {
    'use strict';

    const {WebVICLIRunner} = require('webvicli');
    const webvicliRunner = new WebVICLIRunner();
    await webvicliRunner.run();
}()).catch(function (ex) {
    'use strict';
    console.error(ex);
});
