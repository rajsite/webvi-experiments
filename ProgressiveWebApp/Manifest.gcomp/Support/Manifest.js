
(function () {
    'use strict';

    const register = function () {
        // Intentionally empty, relying on script being included in page at load time
    };

    window.WebVIManifest = {register};
}());
