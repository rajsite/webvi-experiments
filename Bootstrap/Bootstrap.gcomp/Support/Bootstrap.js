(function () {
    'use strict';

    const loadBootstrap = function () {
        // Intentionally empty
        // Need a node to call so bootstrap dependencies are loaded
    };

    window.WebVIBootstrap = {loadBootstrap};
}());
